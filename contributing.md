# Contributing Guide for Quran Management System

مرحباً! 🎉
شكراً على اهتمامك بالمساهمة في مشروع **Quran Management System**.
الهدف من هذا الدليل هو توضيح خطوات المساهمة بشكل احترافي، وضمان أن كل التعديلات واضحة ومهيكلة.

---

## 📌 متطلبات قبل البدء
- معرفة أساسية بـ **Git & GitHub**
- معرفة بـ **React / Supabase** (يفضل)
- الالتزام بأسلوب كتابة الكود المتبع في المشروع
- اختبار التعديلات قبل رفعها

---

## 🧭 خطوات المساهمة

### 1️⃣ Fork المشروع
قم بعمل Fork للمستودع إلى حسابك على GitHub.

### 2️⃣ Clone المشروع محليًا
```bash
git clone https://github.com/mostafa-hessen/Quran_Management_System.git
```

### 3️⃣ إنشاء فرع جديد
> لا تعمل مباشرة على `main`
```bash
git checkout -b feature/your-feature-name
```
**أمثلة:**
- `feature/student-crud`
- `fix/attendance-bug`
- `docs/update-sop`

### 4️⃣ تطوير التعديلات
- كل commit يجب أن يمثل **تغيير واحد واضح**
- اتبع Conventions التالية للـ commits:

```
type(scope): short description
```
**الأنواع:**
- `feat` : إضافة ميزة جديدة
- `fix` : إصلاح خطأ
- `docs` : توثيق فقط
- `style` : تنسيق كود (بدون منطق)
- `refactor` : إعادة هيكلة كود
- `test` : إضافة/تعديل اختبارات
- `chore` : مهام عامة (config, build)

**أمثلة:**
```
feat(attendance): add bulk attendance registration
fix(subscriptions): correct remaining balance calculation
docs(sop): add contributing guide
```

### 5️⃣ الاختبارات
- تأكد أن المشروع يعمل بدون أخطاء
- لا تكسر أي ميزات موجودة
- في حال إضافة Feature جديد، يفضل إضافة اختبار له

### 6️⃣ رفع التعديلات
```bash
git push origin feature/your-feature-name
```
ثم قم بفتح **Pull Request** على GitHub.

---

## 🧾 معايير قبول Pull Request
- وصف واضح للتعديلات
- Commit messages احترافية ومنظمة
- لا توجد أخطاء تشغيل
- الكود منظم ومقروء، ويتبع المعايير الموجودة في المشروع

---

## 📞 التواصل
- في حالة وجود اقتراح أو استفسار، افتح Issue أو تواصل مع Maintainer المشروع.

---

🙏 شكراً لمساهمتك، وجودك يضيف قيمة حقيقية للمشروع!

---

**نصيحة إضافية:**
- كل commit يجب أن يكون صغير وواضح، ويفضل أن يحتوي على Scope لتوضيح جزء المشروع الذي تم تغييره.
- اتبع نفس النمط لجميع مشاريعك على GitHub لتظهر محترف ومرتب.

